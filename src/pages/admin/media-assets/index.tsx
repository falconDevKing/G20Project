import { ContainerFluid } from "@/components/containerFluid";
import MediaAssetsPage from "@/components/media-assets/MediaAssetsPage";

const MediaAssets = () => {
  return (
    <ContainerFluid>
      <MediaAssetsPage />
    </ContainerFluid>
  );
};

export default MediaAssets;
